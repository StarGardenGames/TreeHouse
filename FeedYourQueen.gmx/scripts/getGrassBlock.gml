var xx = argument0;
var yy = argument1;

/*
var scale = .75;
var noise = perlinNoise2D((i/(32*scale) / scale,j/32 * 1.25,100);
if(noise > 40){
    var frame;
    if(noise > 95){
        frame = 4;
    }else{
        frame = (noise - 40) div 15;
    }
    frame = min(4,frame);
    var g = instance_create(i,j,oGrass);
    g.image_index = frame;
    g.image_speed = 0;
}*/
